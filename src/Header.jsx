import styles from "./styles/header.module.scss";

const Header = () => {
  return (
    
      <header className={styles["header"]}>
        ChatApp
      </header>
    
  );
};
export default Header;
